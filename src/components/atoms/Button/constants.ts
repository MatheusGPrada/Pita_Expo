import { theme } from '../../../styles/theme'

export const BUTTON_STYLE = {
    borderRadius: theme.size.m8px,
    borderWidth: theme.size.m1px,
    height: theme.size.m48px,
    letterSpacing: theme.letterSpacing.ls08,
    lineHeight: theme.size.m24px,
}
